package com.example.subsense.core.ui

import androidx.compose.ui.graphics.Color

object LightColors {
    // Background & Surface
    val background = Color(0xFFF7F8FA)
    val foreground = Color(0xFF333E50)
    val card = Color(0xFFFFFFFF)
    val cardForeground = Color(0xFF333E50)

    // Primary (Blue)
    val primary = Color(0xFF2196F3)
    val primaryForeground = Color(0xFFFFFFFF)

    // Secondary
    val secondary = Color(0xFFF0F4FA)
    val secondaryForeground = Color(0xFF333E50)

    // Muted
    val muted = Color(0xFFF0F4FA)
    val mutedForeground = Color(0xFF7A8FA0)

    // Accent (Green)
    val accent = Color(0xFF4CAF50)
    val accentForeground = Color(0xFFFFFFFF)

    // Destructive (Red)
    val destructive = Color(0xFFEF5350)
    val destructiveForeground = Color(0xFFFFFFFF)

    // Warning (Yellow/Amber)
    val warning = Color(0xFFFBC02D)
    val warningForeground = Color(0xFF5D4037)

    // Borders & Input
    val border = Color(0xFFE0E4EA)
    val input = Color(0xFFE0E4EA)
    val ring = Color(0xFF2196F3)
}

object ChartColors {
    val chart1 = Color(0xFF2196F3)
    val chart2 = Color(0xFF4CAF50)
    val chart3 = Color(0xFFFBC02D)
    val chart4 = Color(0xFFEF5350)
    val chart5 = Color(0xFFFF9800)
}
